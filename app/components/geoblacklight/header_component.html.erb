<%= top_bar %>
<% if not_landing_page? %>
<%= search_bar %>
<% else %>
<div class='geobl-homepage-masthead jumbotron'>
    <div class='d-flex justify-content-center'>
      <%= content_tag :h1, t('geoblacklight.home.headline'), class: 'text-center h2' %>
    </div>
    <div class='d-flex justify-content-center'>
      <%= content_tag :h2, t('geoblacklight.home.search_heading'), class: 'text-center h3' %>
    </div>
    <div class='row justify-content-md-center'>
      <div class='col-sm-12 col-md-10'>
        <%= search_bar %>
      </div>
    </div>
  </div>
<% end %>
