# frozen_string_literal: true

require "spec_helper"

RSpec.feature "layer with no wms or iiif" do
  scenario "has map with bounding box" do
    pending "waiting on #201, need example metadata record"
    fail
  end
end
